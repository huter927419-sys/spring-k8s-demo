-- Create database if not exists (this will be handled by MySQL init)
CREATE DATABASE IF NOT EXISTS springk8s;
USE springk8s;

-- Users table will be created by JPA, but we can add initial data
-- This file is for reference, actual schema is managed by JPA

